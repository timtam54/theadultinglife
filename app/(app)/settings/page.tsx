import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Settings",
};

export default function SettingsPage() {
  return <h1 className="font-display text-3xl text-tal-plum">Settings</h1>;
}
