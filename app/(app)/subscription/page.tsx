import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Subscription",
};

export default function SubscriptionPage() {
  return <h1 className="font-display text-3xl text-tal-plum">Subscription</h1>;
}
