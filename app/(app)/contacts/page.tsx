import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contacts",
};

export default function ContactsPage() {
  return <h1 className="font-display text-3xl text-tal-plum">Contacts</h1>;
}
